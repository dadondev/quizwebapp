import Header from "@/components/Header";
import Timer from "@/components/Timer";

const page = () => {
  return (
    <main className="h-full">
      <section>
        <Header />
        <Timer />
      </section>
    </main>
  );
};

export default page;
